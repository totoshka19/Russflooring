// Импортируем функции для настройки обработчиков событий и кнопки отправки
import { setupEventHandlers } from './eventHandlers.js';
import { setupSubmitButton } from './calculations.js';

// Вызываем функцию для настройки обработчиков событий
setupEventHandlers();

// Вызываем функцию для настройки кнопки отправки
setupSubmitButton();
